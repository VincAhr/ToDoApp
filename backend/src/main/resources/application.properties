server.port=${PORT:8080}
spring.data.mongodb.uri=${MONGODB_URI:mongodb://localhost:27017/tododb}
spring.mvc.pathmatch.matching-strategy=ant_path_matcher
app.jwt.secret=${JWT_SECRET:my-super-duper-secret}